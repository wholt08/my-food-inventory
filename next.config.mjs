/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/my-food-inventory',
    images: {
      unoptimized: true,
    }
  }
  
  module.exports = nextConfig